<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mytablayout">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 内存卡 Attempt to get length of null array 加下面两行权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEM" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".TextAppcation"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".materialdesign.TabLayoutActivity" />
        <activity android:name=".materialdesign.CoordinatorActivity" />
        <activity android:name=".materialdesign.CollpsingToolbarActivity" />
        <activity android:name=".view.PointActivity" />
        <activity android:name=".volley.VolleyssActivity" />
        <activity android:name=".okhttp.OkttpssActivity" />
        <activity android:name=".eventbus.EventActivity" />
        <activity android:name=".eventbus.SecondActivity" />
        <activity android:name=".viewpager.ViewPagerActivity" />
        <activity android:name=".searchwenjian.SearchWenJianActivity" />
        <activity android:name=".searchwenjian.FileActivity" />
        <activity android:name=".searchwenjian.TextActivity" />
        <activity android:name=".rxjava.RxJavaActivity" />
        <activity android:name=".rxjava.RxJava2Activity" />
        <activity android:name=".retrofit.Main3Activity" />
        <activity android:name=".view2.LayoutActivity" />
        <activity android:name=".thread.ThreadActivity"></activity>
    </application>

</manifest>